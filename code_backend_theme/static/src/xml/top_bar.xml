<?xml version="1.0" encoding="utf-8"?>
<templates id="template" xml:space="preserve">
    <t t-inherit="web.NavBar" t-inherit-mode="extension" owl="1">
        <xpath expr="//nav[hasclass('o_main_navbar')]" position="replace">
            <nav class="o_main_navbar d-flex justify-content-between" data-command-category="navbar">
                <div class="d-flex">
                    <div class="top_heading">
                        <!-- Apps Menu (en la barra de navegación superior) -->
                        <t t-call="web.NavBar.AppsMenu">
                            <t t-set="apps" t-value="menuService.getApps()"/>
                        </t>
                        <!-- App Brand (nombre de la aplicación en la barra de navegación superior) -->
                        <DropdownItem
                            t-if="currentApp"
                            href="getMenuItemHref(currentApp)"
                            t-esc="currentApp.name"
                            class="'o_menu_brand d-none d-md-block'"
                            dataset="{ menuXmlid: currentApp.xmlid, section: currentApp.id }"
                            onSelected="() => this.onNavBarDropdownItemSelection(currentApp)"
                        />
                    </div>
                </div>
                <!-- Systray -->
                <div class="o_menu_systray d-flex" role="menu">
                    <t t-foreach="systrayItems" t-as="item" t-key="item_index">
                        <ErrorHandler onError="error => handleItemError(error, item)">
                            <t t-component="item.Component" t-props="item.props"/>
                        </ErrorHandler>
                    </t>
                </div>
            </nav>
        </xpath>
    </t>

    <t t-inherit="web.UserMenu" t-inherit-mode="extension" owl="1">
        <xpath expr="//span[hasclass('oe_topbar_name')]" position="replace">
            <div class="oe_topbar_name"/>
        </xpath>
    </t>

    <!-- Barra lateral con menús de aplicaciones y secciones -->
    <t t-inherit="web.Sidebar" t-inherit-mode="extension">
        <xpath expr="//div[@id='app-sidebar']" position="inside">
            <div class="o_sidebar_apps">
                <ul>
                    <t t-foreach="menuService.getApps()" t-as="app" t-key="app_index">
                        <li>
                            <a t-attf-href="#menu_id={{app.id}}"><t t-esc="app.name"/></a>
                            <ul class="o_sidebar_sections">
                                <t t-foreach="app.sections" t-as="section" t-key="section_index">
                                    <li><a t-attf-href="#menu_id={{section.id}}"><t t-esc="section.name"/></a></li>
                                </t>
                            </ul>
                        </li>
                    </t>
                </ul>
            </div>
        </xpath>
    </t>
</templates>